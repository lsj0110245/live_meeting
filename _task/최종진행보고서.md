# LiveMeeting (LM) 프로젝트 진행보고서

**프로젝트명**: LiveMeeting — AI 기반 실시간 회의록 자동 생성 시스템
**작성일**: 2026-03-03
**버전**: v1.0.0

---

## 목차

| 챕터 | 제목 | 주요 내용 |
|------|------|-----------|
| 1장 | 개발 목표 및 범위 (Scope) | 프론트엔드/백엔드 목표, 기술적 방향성 |
| 2장 | 관련 기술 분석 | 기술 스택 선정 이유, 시스템 설계 전략 |
| 3장 | 시스템 설계 | 전체 아키텍처 / DB 스키마 / UI·UX 설계 |
| 4장 | 핵심 구현 상세 | 프론트엔드·백엔드 구현 결과, 인프라 |
| 5장 | 품질 관리 및 보안 전략 | 테스트 전략, 품질 검증, 데이터 보안 |
| 6장 | 주요 기술적 이슈 및 해결 전략 | 트러블슈팅 6가지 사례 |
| 7장 | 성과 분석 및 평가 | 정량적/정성적 성과, 통신 구조 고도화 |
| 8장 | 결론 및 회고 | 최종 요약, 회고·한계·발전 방향 |
| 부록 | 소감문 | 개인 성장, 아쉬운 점, 향후 목표 |

---

# 1장. 개발 목표 및 범위 (Scope)

## 1.1 프로젝트 배경

현대 기업 환경에서 회의는 핵심 업무 활동이나, 회의 내용을 체계적으로 기록·관리하는 데 상당한 시간이 소모된다. 수기 회의록 작성은 인적 오류가 발생하기 쉽고, 핵심 논의사항이 누락되는 경우가 빈번하다. LiveMeeting은 이러한 비효율을 해결하기 위해 **AI 음성 인식(STT)** 과 **대규모 언어모델(LLM)** 을 결합하여, 회의 녹음부터 구조화된 회의록 생성까지의 전 과정을 자동화하는 온프레미스 웹 애플리케이션이다.

## 1.2 개발 목표

### 1.2.1 프론트엔드 목표
- **직관적 UI/UX**: 비개발 직군도 즉시 사용 가능한 랜딩 페이지, 대시보드, 실시간 녹음 페이지, 회의 상세 페이지 구현
- **실시간 상호작용**: WebSocket 기반 실시간 전사(Transcript) 표시, 중간 요약 자동 갱신
- **반응형 디자인**: CSS 변수 기반 다크 테마, 글래스모피즘(Glassmorphism) UI, 모바일 대응
- **파일 관리**: 폴더 기반 회의 분류, 일괄 선택·삭제·이동, 드래그 앤 드롭 파일 업로드

### 1.2.2 백엔드 목표
- **고성능 비동기 처리**: FastAPI 기반 비동기 API 서버 구축
- **온프레미스 AI 파이프라인**: 외부 클라우드 의존 없이 Faster-Whisper(STT) + EXAONE 3.5/Ollama(LLM)를 GPU 서버에서 직접 구동
- **견고한 데이터 관리**: PostgreSQL + SQLAlchemy ORM, Alembic 마이그레이션 기반 스키마 진화
- **보안**: JWT 인증, bcrypt 비밀번호 해싱, CORS 정책 제어

### 1.2.3 기술적 방향성
- **데이터 주권 확보**: 모든 음성 데이터와 회의록을 로컬 서버에 저장하여 기업 보안 정책 준수
- **확장 가능 아키텍처**: Docker Compose 기반 멀티 컨테이너 오케스트레이션으로 서비스 단위 독립 배포·확장 가능
- **AI 모델 교체 용이성**: `.env` 환경변수를 통한 STT 모델 크기, LLM 모델명, GPU 설정 등 런타임 변경 지원

## 1.3 개발 범위

| 구분 | 포함 범위 | 비고 |
|------|-----------|------|
| 프론트엔드 | 랜딩 페이지, 로그인/회원가입, 대시보드(폴더 관리), 실시간 녹음, 회의 상세, 프로필, CSV/XLSX 내보내기 | Jinja2 + Vanilla JS |
| 백엔드 API | 인증(auth), 사용자(users), 폴더(folders), 녹음(recording), 업로드(upload), 회의(meeting), 내보내기(export), 진행률(progress) | FastAPI 8개 라우터 |
| AI 서비스 | STT(Faster-Whisper large-v3-turbo), LLM 요약(EXAONE 3.5 via Ollama), 텍스트 교정 | 온프레미스 GPU |
| 인프라 | Docker Compose (PostgreSQL + Backend + Ollama), NVIDIA GPU 할당 | pgvector 확장 포함 |
| 품질 관리 | pytest 단위 테스트, 수동 검증, Alembic 마이그레이션 이력 관리(11개 버전) | CI/CD 미포함 |

---

# 2장. 관련 기술 분석

## 2.1 기술 스택 선정 이유

### 2.1.1 백엔드 프레임워크: FastAPI

| 선정 기준 | FastAPI | Django REST | Flask |
|-----------|---------|-------------|-------|
| 비동기 지원 | ✅ 네이티브 async/await | ⚠️ ASGI 별도 설정 | ❌ WSGI 기반 |
| WebSocket | ✅ 내장 지원 | ⚠️ Channels 추가 필요 | ⚠️ Flask-SocketIO |
| 자동 문서 | ✅ Swagger + ReDoc | ⚠️ drf-yasg | ❌ 별도 구성 |
| 성능 | ✅ Starlette 기반 고성능 | ⚠️ 보통 | ⚠️ 보통 |

**선정 사유**: 실시간 WebSocket 통신과 GPU 기반 AI 추론이 핵심 요구사항이므로, 네이티브 비동기를 지원하며 WebSocket이 내장된 FastAPI를 채택함. `asyncio.to_thread()`를 활용한 CPU-bound 작업의 이벤트 루프 분리가 용이함.

### 2.1.2 STT 엔진: Faster-Whisper (Large-v3-Turbo)

| 항목 | Faster-Whisper | OpenAI Whisper | Google STT |
|------|---------------|----------------|------------|
| 속도 | ✅ CTranslate2 기반 4~5배 빠름 | ⚠️ 기본 속도 | ✅ 빠름 |
| 메모리 | ✅ ~3.5GB VRAM | ❌ ~6GB VRAM | N/A (클라우드) |
| 데이터 주권 | ✅ 완전 로컬 | ✅ 완전 로컬 | ❌ 클라우드 전송 |
| 한국어 정확도 | ✅ large-v3-turbo로 우수 | ✅ 우수 | ✅ 우수 |

**선정 사유**: 온프레미스 운영 필수 조건 충족. CTranslate2 기반 최적화로 동일 VRAM에서 더 빠른 추론 속도 달성. `deepdml/faster-whisper-large-v3-turbo-ct2` 모델로 한국어 전문 용어(API, Docker, LLM 등)의 인식 정확도가 medium 대비 현저하게 향상됨.

### 2.1.3 LLM 서비스: EXAONE 3.5 (7.8B) via Ollama

- **한국어 특화**: LG AI Research의 EXAONE 3.5은 한국어 비즈니스 문서 생성에 최적화
- **Ollama 래퍼**: Docker 컨테이너 내에서 `ollama/ollama:latest` 이미지로 간편하게 모델 서빙
- **LangChain 통합**: `ChatOllama` + `ChatPromptTemplate` + `StrOutputParser` 체인으로 구조화된 JSON 회의록 생성
- **컨텍스트 윈도우**: `num_ctx=16384` 설정으로 긴 회의 전사 텍스트 처리 가능

### 2.1.4 데이터베이스: PostgreSQL + pgvector

- **관계형 데이터 모델링**: 사용자-회의-전사-요약 간의 명확한 1:N, 1:1 관계 표현
- **pgvector 확장**: 향후 임베딩 기반 유사 회의 검색을 위한 확장성 확보
- **Alembic 마이그레이션**: 11개 버전 이력으로 스키마 변경 추적

### 2.1.5 프론트엔드: Jinja2 + Vanilla JavaScript

- **서버 사이드 렌더링(SSR)**: FastAPI의 Jinja2 템플릿 엔진 활용으로 SEO 및 초기 로딩 속도 확보
- **SPA 유사 동작**: JavaScript를 통해 Toast 알림, 모달, 실시간 전사 업데이트 등 동적 UI 구현
- **의존성 최소화**: React/Vue 등 프레임워크 없이 순수 JS로 구현하여 빌드 파이프라인 불필요

## 2.2 시스템 설계 전략

### 2.2.1 온프레미스 AI 파이프라인 전략

```
[사용자 브라우저] ──WebSocket──▶ [FastAPI Backend] ──GPU──▶ [Faster-Whisper STT]
                                      │                        │
                                      ▼                        ▼
                              [PostgreSQL DB]          [전사 텍스트 저장]
                                      │
                                      ▼
                              [Ollama/EXAONE LLM] ──▶ [구조화된 회의록 JSON]
```

- 모든 AI 처리가 도커 네트워크 내부(`lm_network`)에서 수행됨
- 외부 API 호출 없이 완전한 독립 망 운영 가능
- GPU 자원을 STT와 LLM이 시분할로 공유 (VRAM 6GB 환경 기준)

### 2.2.2 실시간 처리 vs 배치 처리 이원화

| 구분 | 실시간 녹음 | 파일 업로드 |
|------|-------------|-------------|
| STT 전략 | 5초 버퍼링, beam_size=5, Denoise 생략 | 30초 청킹, beam_size=10, 고품질 Denoise |
| 지연 시간 | <1초 (속도 우선) | 수분 (정확도 우선) |
| 전사 교정 | 실시간 교정 비활성화 (속도) | LLM 문맥 교정 적용 |
| 요약 생성 | 3분마다 중간 요약 + 종료 시 최종 요약 | STT 완료 후 일괄 요약 |

---

# 3장. 시스템 설계

## 3.1 전체 아키텍처

### 3.1.1 Docker Compose 서비스 구성

```yaml
services:
  db:        # PostgreSQL + pgvector (포트 15432:5432)
  backend:   # FastAPI + Faster-Whisper STT (포트 8001:8000, GPU)
  llm:       # Ollama/EXAONE 3.5 (포트 11434:11434, GPU)
```

- **db**: `pgvector/pgvector:pg18` 이미지, 영구 볼륨(`postgres_data`) 마운트
- **backend**: 커스텀 Dockerfile, Python 3.11 기반, NVIDIA GPU 1장 할당, `start.sh`로 Alembic 마이그레이션 후 uvicorn 실행
- **llm**: `ollama/ollama:latest`, 커스텀 엔트리포인트(`ollama_entrypoint.sh`)로 EXAONE 3.5 모델 자동 pull

### 3.1.2 디렉토리 구조

```
live_meeting/
├── backend/
│   ├── app/
│   │   ├── api/endpoints/     # 8개 API 라우터
│   │   ├── core/              # config.py, security.py
│   │   ├── db/                # session.py, base.py
│   │   ├── models/            # 6개 SQLAlchemy 모델
│   │   ├── schemas/           # 4개 Pydantic 스키마
│   │   ├── services/          # STT, LLM, Meeting Tasks, Progress
│   │   └── main.py            # FastAPI 앱 진입점
│   ├── alembic/versions/      # 11개 DB 마이그레이션
│   └── requirements.txt       # 25+ Python 패키지
├── frontend/
│   ├── static/
│   │   ├── css/style.css      # 글로벌 스타일 (다크 테마)
│   │   ├── js/                # 6개 JavaScript 모듈
│   │   └── images/            # 히어로 이미지 등
│   └── templates/             # 7개 Jinja2 HTML 템플릿 + 컴포넌트
├── docker-compose.yml
└── .env                       # 환경변수 (SECRET_KEY, DB 정보, AI 설정)
```

## 3.2 데이터베이스 스키마

### 3.2.1 ERD (Entity-Relationship Diagram)

```
[Users] 1──N [Meetings] 1──N [Transcripts]
   │              │
   │              ├── 1──1 [Summaries]
   │              │
   │              └── 1──N [IntermediateSummaries]
   │
   └── 1──N [Folders] 1──N [Meetings]
```

### 3.2.2 테이블 상세

#### Users 테이블
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | Integer, PK | 사용자 고유 ID |
| email | String, UNIQUE | 이메일 (로그인 ID) |
| username | String | 사용자 이름 |
| hashed_password | String | bcrypt 해싱된 비밀번호 |
| is_active | Boolean | 계정 활성 상태 |
| age, phone_number, team_name | String | 프로필 추가 정보 |
| profile_image_path | String | 프로필 이미지 경로 |

#### Meetings 테이블
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | Integer, PK | 회의 고유 ID |
| title | String | 회의 제목 (LLM 자동 제안 가능) |
| description | Text | 회의 목적/설명 |
| owner_id | Integer, FK→users | 소유자 |
| status | String | pending / recording / processing / completed / failed |
| audio_file_path | String | 오디오 파일 상대/절대 경로 |
| file_hash | String(64) | SHA-256 해시 (중복 업로드 방지) |
| folder_id | Integer, FK→folders | 폴더 분류 |
| duration | Integer | 녹음 시간 (초) |
| meeting_type | String | 회의 유형 (LLM 자동 추출) |
| meeting_date | DateTime | 회의 일시 |
| attendees | Text | 참석자 (LLM 자동 추출) |
| writer | String | 작성자 |

#### Transcripts 테이블
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | Integer, PK | 전사 세그먼트 ID |
| meeting_id | Integer, FK→meetings | 회의 참조 |
| segment_index | Integer | 세그먼트 순서 |
| start_time | Float | 시작 시간 (초) |
| end_time | Float | 종료 시간 (초) |
| text | Text | 전사된 텍스트 |
| speaker | String | 화자 (향후 Diarization 대비) |

#### Summaries 테이블 (1:1 관계)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | Integer, PK | 요약 ID |
| meeting_id | Integer, FK→meetings, UNIQUE | 회의 참조 (1:1) |
| content | Text | Markdown 형식 회의록 |
| created_at, updated_at | DateTime | 타임스탬프 |

#### IntermediateSummaries 테이블 (1:N 관계)
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | Integer, PK | 중간 요약 ID |
| meeting_id | Integer, FK→meetings | 회의 참조 |
| content | Text | 중간 요약 텍스트 |
| created_at | DateTime | 생성 시간 |

#### Folders 테이블
| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | Integer, PK | 폴더 ID |
| name | String | 폴더 이름 |
| owner_id | Integer, FK→users | 소유자 |

## 3.3 API 설계

### 3.3.1 RESTful API 엔드포인트

| 메서드 | 경로 | 설명 | 인증 |
|--------|------|------|------|
| POST | `/api/auth/register` | 회원가입 | ❌ |
| POST | `/api/auth/login` | 로그인 (JWT 발급) | ❌ |
| GET | `/api/users/me` | 내 정보 조회 | ✅ |
| PUT | `/api/users/me` | 프로필 수정 | ✅ |
| GET | `/api/folders/` | 폴더 목록 | ✅ |
| POST | `/api/folders/` | 폴더 생성 | ✅ |
| WS | `/api/recording/ws/{client_id}` | 실시간 녹음 WebSocket | ✅ (Query Token) |
| POST | `/api/upload/file` | 오디오 파일 업로드 | ✅ |
| POST | `/api/upload/recording/{id}/finalize` | 녹음 파일 최종 저장 | ✅ |
| POST | `/api/upload/recording/{id}/concat-resume` | 이어서 녹음 합치기 | ✅ |
| GET | `/api/meeting/` | 회의 목록 조회 | ✅ |
| GET | `/api/meeting/{id}` | 회의 상세 조회 | ✅ |
| PUT | `/api/meeting/{id}` | 회의 정보 수정 | ✅ |
| DELETE | `/api/meeting/{id}` | 회의 삭제 | ✅ |
| DELETE | `/api/meeting/` | 일괄 삭제 | ✅ |
| POST | `/api/meeting/{id}/summarize` | 회의록 재생성 | ✅ |
| POST | `/api/meeting/{id}/retry` | STT 재분석 | ✅ |
| GET | `/api/export/{id}?format=csv|xlsx` | 내보내기 | ✅ |
| GET | `/api/progress/{id}` | 처리 진행률 조회 | ✅ |

### 3.3.2 WebSocket 프로토콜 설계

```
Client → Server (JSON):
  { "type": "metadata", "data": { "title", "meeting_type", "meeting_date", "attendees", "writer", "meeting_id" } }
  { "type": "stop_recording" }

Client → Server (Binary):
  [WebM 오디오 청크 바이너리 데이터]

Server → Client (JSON):
  { "type": "connected", "message": "..." }
  { "type": "meeting_created", "meeting_id": N }
  { "type": "transcript", "transcript_id": N, "text": "...", "is_final": true }
  { "type": "transcript_update", "transcript_id": N, "text": "..." }
  { "type": "intermediate_summary", "content": "..." }
  { "type": "error", "message": "..." }
```

## 3.4 UI·UX 설계

### 3.4.1 화면 구성 (7개 페이지)

| 페이지 | 템플릿 | JS 모듈 | 주요 기능 |
|--------|--------|---------|-----------|
| 랜딩 | `index.html` | `dashboard.js` | 히어로 섹션, 기능 소개, CTA |
| 로그인 | `login.html` | `auth.js` | 이메일/비밀번호 인증, JWT 저장 |
| 회원가입 | `register.html` | `auth.js` | 이메일/이름/비밀번호 검증 |
| 대시보드 | `index.html` | `dashboard.js` | 폴더 사이드바, 회의 카드 그리드, 업로드, 일괄 관리 |
| 실시간 녹음 | `recording.html` | `recording.js` | WebSocket STT, 실시간 자막, 오디오 녹음, 메타데이터 입력 |
| 회의 상세 | `meeting_detail.html` | `meeting.js` | 오디오 재생, 전사 타임라인, AI 요약, 수정, 내보내기 |
| 프로필 | `profile.html` | `profile.js` | 사용자 정보 수정, 이미지 업로드 |

---

# 4장. 핵심 구현 상세

## 4.1 프론트엔드 엔지니어링

### 4.1.1 대시보드 (`dashboard.js` — 44,153 bytes)

**핵심 구현 내용:**
- **폴더 기반 회의 관리**: 사이드바에서 폴더 생성/삭제, 회의를 폴더로 드래그하여 분류
- **카드형 회의 목록**: 각 회의 카드에 제목, 날짜, 상태 배지(recording/processing/completed/failed) 표시
- **일괄 작업**: 체크박스 선택 → 일괄 삭제, 폴더 이동, 폴더에서 제외
- **파일 업로드**: 드래그 앤 드롭 + 클릭 업로드, 메타데이터(제목/참석자/작성자) 입력 모달
- **진행률 폴링**: `setInterval`로 `/api/progress/{id}` 주기적 조회, 프로그레스 바 표시
- **필터링**: 전체 회의 / 미분류 / 특정 폴더별 필터링

### 4.1.2 실시간 녹음 (`recording.js` — 34,759 bytes)

**핵심 구현 내용:**
- **MediaRecorder API**: `getUserMedia()`로 마이크 오디오 스트림 캡처, `timeslice: 500ms`로 0.5초 단위 청크 생성
- **WebSocket 연결**: JWT 토큰을 Query Parameter로 전달하여 인증, 연결 상태 모니터링
- **실시간 자막 표시**: 서버에서 수신한 `transcript` 이벤트를 DOM에 실시간으로 추가
- **중간 요약 표시**: `intermediate_summary` 이벤트 수신 시 사이드 패널에 요약 갱신
- **이어서 녹음하기**: 기존 회의 ID를 전달하여 이전 세션 이어서 녹음, 서버에서 타임스탬프 연속성 확보
- **오디오 파일 최종 저장**: 녹음 종료 시 `MediaRecorder`의 완성된 Blob을 `/upload/recording/{id}/finalize`로 POST
- **에러 핸들링**: `isIntentionalStop` 플래그로 의도적 종료 vs 비정상 연결 끊김 구분

### 4.1.3 회의 상세 (`meeting.js` — 23,036 bytes)

**핵심 구현 내용:**
- **오디오 플레이어**: `<audio>` 태그 + 커스텀 컨트롤 바, Range Request(HTTP 206) 지원으로 시크(Seek) 가능
- **전사 타임라인**: 세그먼트별 시작/종료 시간 표시, 클릭 시 해당 시점으로 오디오 이동
- **AI 요약 표시**: Markdown → HTML 변환, 섹션별(📅 요약, 📌 안건, ✅ 결정, 📝 향후 계획) 구조화 렌더링
- **메타데이터 수정**: 제목, 회의 유형, 참석자 등 인라인 수정 → PUT `/api/meeting/{id}`
- **내보내기**: CSV/XLSX 다운로드 버튼, `source.xlsx` 템플릿 기반 엑셀 생성

### 4.1.4 인증 (`auth.js` — 3,750 bytes)
- 로그인/회원가입 폼 처리
- JWT 토큰 `localStorage` 저장
- `common.js`의 `fetchWithAuth()` 헬퍼로 모든 API 요청에 `Authorization: Bearer {token}` 자동 첨부

## 4.2 백엔드 고도화

### 4.2.1 STT 서비스 (`faster_whisper_stt_service.py` — 555줄)

**3-Tier STT 파이프라인:**

1. **실시간 전사 (`transcribe_realtime`)**
   - 5초 버퍼 축적 → ffmpeg stdin pipe로 WebM→WAV 변환 → Faster-Whisper GPU 추론
   - `beam_size=5`, `temperature=0`, `repetition_penalty=1.3`
   - VAD(Voice Activity Detection) 필터: `min_silence_duration_ms=800`, `threshold=0.5`
   - **환각 필터링**: RegEx 패턴으로 "자막", "감사합니다", "마이크 테스트" 등 무의미한 출력 제거
   - `no_speech_prob > 0.8` 침묵 구간 자동 스킵

2. **파일 전사 — 단일 패스 (`transcribe_file`)**
   - `beam_size=10`, `best_of=10`으로 정확도 극대화
   - `condition_on_previous_text=True`로 문맥 연속성 확보
   - 전체 파일을 한 번에 처리 (비교적 짧은 파일용)

3. **파일 전사 — 청킹 패스 (`transcribe_file_chunked`)**
   - 30초 청크 + 5초 오버랩으로 긴 파일 처리
   - `_merge_overlapping_segments()`로 오버랩 구간 중복 텍스트 자동 병합
   - 진행률 콜백을 통한 실시간 처리 상황 업데이트

**오디오 전처리 (`_preprocess_audio`):**
- **파일 모드 (high)**: noisereduce 라이브러리로 Stationary Noise 제거 → pydub Normalize → +5dB 증폭
- **실시간 모드 (fast)**: Denoise 생략, Normalize만 수행 (WebSocket 타임아웃 방지)
- **Fallback 체인**: pydub 실패 → ffmpeg pipe → 무음 WAV 반환 (오류 전파 차단)

### 4.2.2 LLM 서비스 (`llm_service.py` — 253줄)

**3가지 LLM 기능:**

1. **구조화된 회의록 생성 (`generate_summary`)**
   - LangChain `ChatPromptTemplate` + `StrOutputParser` 체인
   - JSON 출력 포맷: `{ metadata: { title_suggestion, meeting_type, attendees }, summary: { purpose, content, conclusion, action_items } }`
   - **3단계 JSON 파싱 Fallback**: `json.loads()` → `ast.literal_eval()` → RegEx 추출
   - 파싱 실패 시 원본 텍스트를 포함한 기본 구조 반환 (사용자에게 빈 화면 방지)

2. **중간 요약 (`generate_simple_summary`)**
   - 3줄 이내 간결 요약, 순수 텍스트 반환
   - 실시간 녹음 중 3분 간격으로 자동 트리거

3. **전사 텍스트 교정 (`correct_transcript`)**
   - 도메인 용어 사전(API, Docker, FastAPI, STT 등) 기반 음성 유사어 오인식 방지
   - 숫자/기호 정규화 (전화번호, 날짜 형식)
   - 사족 필터링: "교정 결과:", "수정했습니다" 등 LLM이 추가하는 불필요 텍스트 제거

### 4.2.3 실시간 녹음 WebSocket (`recording.py` — 752줄)

**핵심 아키텍처:**

- **`RealtimeSession` 클래스**: 세션별 오디오 버퍼, 전사 이력, 메타데이터, WebM 헤더 관리
- **`ConnectionManager` 클래스**: 다중 클라이언트 WebSocket 연결 관리, 전송 실패 시 `disconnected_clients` 추적
- **이벤트 루프 보호**: 모든 GPU 연산은 `asyncio.to_thread()`로 별도 스레드에서 실행하여 WebSocket Ping/Pong 차단 방지
- **이어서 녹음**: `is_resume_session` 플래그, 기존 `segment_index`와 `total_duration` 이어받기, 별도 임시 파일 저장 후 FFmpeg `concat` 병합
- **최종 정리**: `final_cleanup_and_summary()` 비동기 태스크에서 잔여 버퍼 처리 → duration 업데이트 → LLM 요약 생성 → meeting 상태 `COMPLETED` 전환

### 4.2.4 파일 업로드 처리 (`upload.py` — 384줄)

- **SHA-256 중복 방지**: 업로드 시 파일 해시 계산, 기존 동일 파일 발견 시 재사용
- **BackgroundTask 기반 비동기 STT**: `process_audio_file()` 함수를 백그라운드에서 실행 → STT → 세그먼트별 LLM 교정 → DB 저장 → 자동 요약 생성
- **진행률 관리**: `progress_service`를 통한 0~100% 진행률 업데이트 (STT 0~90%, 요약 90~100%)
- **finalize 엔드포인트**: 실시간 녹음 종료 후 완성된 WebM 파일 수신, FFmpeg Duration 헤더 복구

### 4.2.5 내보내기 서비스 (`export.py` — 236줄)

- **CSV**: `pandas` + `utf-8-sig` BOM으로 한글 인코딩 보장
- **XLSX**: `openpyxl`로 `source.xlsx` 템플릿 기반 생성, 셀 스타일(줄바꿈, 정렬, 열 너비) 자동 설정
- **AI 요약 파싱**: Markdown 섹션별(📅📌✅📝) RegEx 파싱 → 엑셀 셀 분배
- **엑셀 수식 오류 방지**: 대시(`-`) 불렛을 `●`로 치환 (엑셀이 `-`를 수식으로 해석하는 문제 방지)

### 4.2.6 Map-Reduce 요약 전략 (`meeting_tasks.py` — 251줄)

- **안전 제한**: 전사 텍스트 10,000자 초과 시 Map-Reduce 전략 자동 전환
- **Map**: 3,000자 청크 + 500자 오버랩으로 분할 → 각 청크별 `generate_simple_summary()` 호출
- **Reduce**: 부분 요약들을 통합하여 `generate_summary()`로 최종 구조화된 회의록 생성
- **진행률 시각화**: `slow_climb()` 비동기 태스크로 2초마다 1%씩 진행률 상승 (사용자 체감 UX 향상)

## 4.3 인프라

### 4.3.1 Docker Compose 구성

| 서비스 | 이미지 | 포트 | GPU | 볼륨 |
|--------|--------|------|-----|------|
| db | `pgvector/pgvector:pg18` | 15432:5432 | ❌ | `postgres_data` (영구) |
| backend | 커스텀 (Python 3.11) | 8001:8000 | ✅ (1장) | `./backend`, `./frontend`, `./media`, `./exports` |
| llm | `ollama/ollama:latest` | 11434:11434 | ✅ (1장) | `./ai_models` |

### 4.3.2 환경변수 관리 (`.env`)

| 카테고리 | 주요 변수 | 설명 |
|----------|-----------|------|
| DB | `POSTGRES_USER`, `POSTGRES_PASSWORD`, `DATABASE_URL` | PostgreSQL 연결 정보 |
| JWT | `SECRET_KEY`, `ALGORITHM`, `ACCESS_TOKEN_EXPIRE_MINUTES` | 인증 토큰 설정 |
| STT | `STT_MODEL_SIZE`, `STT_DEVICE`, `STT_COMPUTE_TYPE` | Whisper 모델 설정 |
| LLM | `LLM_MODEL`, `LLM_TEMPERATURE`, `OLLAMA_BASE_URL` | EXAONE/Ollama 설정 |
| 보안 | `CORS_ORIGINS`, `MAX_FILE_SIZE_MB`, `ALLOWED_EXTENSIONS` | 접근 제어 |

---

# 5장. 품질 관리 및 보안 전략

## 5.1 테스트 전략

### 5.1.1 테스트 계층 구조

| 계층 | 도구 | 대상 | 상태 |
|------|------|------|------|
| 단위 테스트 | pytest + pytest-asyncio | 서비스 함수 (STT, LLM, 유틸리티) | 구현됨 |
| API 테스트 | FastAPI TestClient | RESTful 엔드포인트 (CRUD, 인증) | 구현됨 |
| 통합 테스트 | 수동 검증 | 실시간 녹음 → STT → LLM → 회의록 전체 플로우 | 수동 수행 |
| 부하 테스트 | (계획) Locust/JMeter | 동시 WebSocket 연결, 대용량 파일 업로드 | 미구현 |

### 5.1.2 핵심 테스트 시나리오

1. **인증 플로우**: 회원가입 → 로그인 → JWT 토큰 발급 → 인증된 API 호출 → 토큰 만료 시 403 반환
2. **파일 업로드 플로우**: 파일 확장자 검증 → SHA-256 해시 중복 체크 → STT 백그라운드 처리 → DB 저장 → 자동 요약
3. **실시간 녹음 플로우**: WebSocket 연결 → 메타데이터 전송 → 오디오 청크 스트리밍 → 5초 버퍼 전사 → 3분 중간 요약 → 종료 → 최종 요약
4. **이어서 녹음 플로우**: 기존 meeting_id 전달 → segment_index 이어받기 → 별도 임시 파일 저장 → FFmpeg concat → duration 합산
5. **내보내기 플로우**: CSV(utf-8-sig) / XLSX(openpyxl 템플릿) 정상 다운로드, 한글 파일명 인코딩 확인

### 5.1.3 STT 품질 검증

| 테스트 환경 | 입력 | 기대 결과 | 실제 결과 |
|-------------|------|-----------|-----------|
| 일반 대화 (1m 거리) | 한국어 비즈니스 회의 | 문맥에 맞는 자연스러운 전사 | ✅ 정확도 양호 |
| 소음 환경 (에어컨) | 배경 잡음 + 음성 | 잡음 제거 후 음성만 인식 | ✅ noisereduce 적용 시 개선 |
| 작은 목소리 (1m+) | 속삭이는 음성 | +5dB 증폭 후 인식 | ✅ Normalize 적용 후 인식됨 |
| 전문 용어 | "LLM", "FastAPI", "Docker" | 영문 원어 그대로 전사 | ✅ large-v3-turbo로 정확히 인식 |
| 긴 회의 (10분+) | 연속 녹음 | WebSocket 연결 유지, 전사 누적 | ✅ Denoise 생략으로 안정화 |

## 5.2 품질 검증 결과

### 5.2.1 DB 마이그레이션 이력 (Alembic)

총 **11개 마이그레이션 버전**이 관리되며, 스키마 진화 과정이 추적됨:

| # | 버전 해시 | 내용 |
|---|-----------|------|
| 1 | `f55db5cb4819` | User 모델 생성 |
| 2 | `a8981f3b4ff3` | Meeting, Transcript, Summary 모델 생성 |
| 3 | `375f9b500b8c` | Users 테이블에 username 컬럼 추가 |
| 4 | `7c7550e2d45b` | username 추가 재시도 (마이그레이션 충돌 해결) |
| 5 | `1bf0a1ef0b85` | Meeting에 audio_file_path 컬럼 추가 |
| 6 | `7c78715da1d6` | Meeting에 status 컬럼 추가 |
| 7 | `90f4345f2a1a` | Meeting에 file_hash 컬럼 추가 (중복 방지) |
| 8 | `a0ff3c623e1e` | Meeting에 누락된 메타데이터 컬럼 일괄 추가 |
| 9 | `aead48c7fdac` | Folder 모델 추가 (v2) |
| 10 | `b79f55bcff7d` | IntermediateSummaries 테이블 생성 |
| 11 | `d66a257e837e` | User 프로필 필드 추가 (age, phone, team, image) |

### 5.2.2 코드 품질 지표

| 항목 | 수치 |
|------|------|
| Backend 총 Python 소스 라인 | ~3,500줄 |
| Frontend JS 총 라인 | ~3,500줄 (6개 모듈) |
| API 엔드포인트 수 | 18개 (8개 라우터) |
| DB 모델 수 | 6개 |
| Pydantic 스키마 수 | 4개 |
| 서비스 모듈 수 | 5개 |

## 5.3 데이터 보안

### 5.3.1 인증 및 권한 관리

| 보안 계층 | 구현 내용 |
|-----------|-----------|
| **비밀번호 해싱** | passlib + bcrypt 알고리즘 (`CryptContext(schemes=["bcrypt"])`) |
| **JWT 토큰** | python-jose, HS256 알고리즘, 300분(5시간) 만료 |
| **토큰 검증** | `deps.get_current_user()` 의존성 주입으로 모든 보호 엔드포인트에 자동 적용 |
| **소유권 검증** | 회의 CRUD 시 `meeting.owner_id == current_user.id` 검증 (타 사용자 데이터 접근 차단) |
| **WebSocket 인증** | Query Parameter로 JWT 전달 → 연결 수립 전 검증 → 실패 시 `WS_1008_POLICY_VIOLATION` 코드로 거절 |

### 5.3.2 데이터 보호

| 항목 | 구현 내용 |
|------|-----------|
| **CORS 정책** | `settings.cors_origins_list`로 허용 도메인 명시적 관리 |
| **파일 업로드 제한** | 확장자: mp3, wav, m4a, mp4, webm, avi, mov만 허용 / 최대 크기: 500MB |
| **SQL Injection 방지** | SQLAlchemy ORM 사용 (Raw SQL 미사용) |
| **환경변수 분리** | `.env` 파일로 SECRET_KEY, DB 비밀번호 등 민감 정보 관리, `.gitignore`에 포함 |
| **파일 해시 중복 방지** | SHA-256 해시로 동일 파일 재업로드 감지 → 기존 파일 재사용 |
| **완전 온프레미스** | 모든 AI 처리(STT, LLM)가 로컬 서버에서 수행 → 외부 클라우드로 데이터 전송 없음 |
| **Cascade 삭제** | 회의 삭제 시 관련 전사, 요약, 중간요약 자동 삭제 (`cascade="all, delete-orphan"`) |

---

# 6장. 주요 기술적 이슈 및 해결 전략

## 6.1 이슈 1: 실시간 녹음 중 WebSocket 연결 끊김

### 증상
- 실시간 녹음 3~5분 경과 후 "서버 연결이 끊겨 녹음이 중단되었습니다" 알림 발생

### 원인 분석
- `faster_whisper_stt_service.py`의 `_preprocess_audio()` 함수에서 **매 청크마다 noisereduce 라이브러리의 고비용 CPU 연산** 수행
- 이벤트 루프가 차단(Blocking)되어 WebSocket Ping/Pong 응답 불가 → 타임아웃 발생

### 해결 전략
```python
# [Before] 무조건 잡음 제거 수행
reduced_noise_audio = nr.reduce_noise(y=samples, sr=16000, ...)

# [After] quality 파라미터로 분기 처리
if quality == "high":    # 파일 전사용: 정확도 우선
    reduced_noise_audio = nr.reduce_noise(y=samples, sr=16000, ...)
else:                    # 실시간용: 속도 우선, Denoise 생략
    reduced_noise_audio = samples
```

### 결과
- CPU 부하 대폭 감소, WebSocket 연결 끊김 없이 장시간 실시간 녹음 가능
- 최종 회의록 품질은 녹음 종료 후 파일 업로드 재분석(Finalize)으로 유지

## 6.2 이슈 2: STT 환각(Hallucination) 텍스트 출력

### 증상
- 실시간 전사 시 "자막 박진희", "시청해 주셔서 감사합니다", "아자차카타파하" 등 무의미한 텍스트 반복 생성

### 원인 분석
- Whisper 모델의 `initial_prompt`에 테스트용 예시 문구가 포함되어 있어 모델이 이를 그대로 따라함
- 무음·잡음 구간에서 `no_speech_prob`이 높아도 텍스트를 강제 생성하는 현상

### 해결 전략
1. **Initial Prompt 정제**: 테스트 예시 제거 → "회의 녹음입니다. 자연스러운 한국어 문장으로 기록해 주세요."
2. **RegEx 필터링 추가**: 환각 패턴 감지 후 스킵
3. **침묵 감지**: `no_speech_prob > 0.8` 세그먼트 자동 제거
4. **no_repeat_ngram_size=3, repetition_penalty=1.3** 적용으로 반복 생성 억제

### 결과
- 환각 텍스트 99% 이상 제거, 실제 발화 내용만 전사

## 6.3 이슈 3: WebM 스트리밍 청크의 불완전한 헤더 문제

### 증상
- 실시간 녹음 시 두 번째 청크부터 EBML 헤더가 없어 pydub/ffmpeg가 파싱 실패

### 원인 분석
- MediaRecorder의 WebM 출력은 첫 번째 청크에만 EBML 헤더를 포함
- 이후 청크는 헤더 없이 데이터만 전송되어 독립적으로 디코딩 불가

### 해결 전략
1. **헤더 캐싱**: `RealtimeSession.header_bytes`에 첫 번째 청크(헤더 포함)를 저장
2. **헤더 병합**: 두 번째 청크부터 `header_bytes + current_data`로 합쳐서 전사
3. **skip_duration_ms**: 헤더 재결합으로 인한 중복 구간은 ffmpeg `-ss` 옵션으로 스킵
4. **ffmpeg stdin pipe**: pydub(파일 경유) 대신 `subprocess.run(cmd, input=audio_bytes)`로 직접 변환 → 불완전한 EBML도 관대하게 처리

### 결과
- 실시간 스트리밍 오디오 청크의 안정적 WAV 변환 달성

## 6.4 이슈 4: JWT 토큰 만료 시 UX 단절

### 증상
- 장시간 녹음 중 JWT 토큰이 만료되면 이후 API 호출 실패, 사용자에게 적절한 안내 없음

### 해결 전략
- `ACCESS_TOKEN_EXPIRE_MINUTES = 300` (5시간)으로 설정하여 일반 회의 시간 충분히 커버
- `common.js`의 `fetchWithAuth()`에서 403 응답 감지 시 로그인 페이지로 리다이렉트
- WebSocket 연결은 연결 수립 시점의 토큰만 검증 (연결 유지 중 만료 시에는 기존 세션 유지)

### 결과
- 토큰 만료 시 자연스러운 재인증 유도, 녹음 중 데이터는 서버에 이미 저장되어 손실 없음

## 6.5 이슈 5: 회의 상태 조기 완료 처리 (데이터 정합성)

### 증상
- 녹음 종료 직후 회의 상태가 `completed`로 변경되나, LLM 요약이 아직 생성되지 않아 빈 회의록 표시

### 원인 분석
- `recording.py`의 `final_cleanup_and_summary()`에서 요약 생성 전에 상태를 `completed`로 설정

### 해결 전략
- **상태 전이 순서 변경**: `recording` → `processing` → (요약 완료 후) `completed`
- `meeting_tasks.py`의 `process_meeting_summary()`에서 모든 처리 완료 후에만 `MeetingStatus.COMPLETED` 설정
- 에러 발생 시 `_force_complete_meeting_sync()`로 강제 완료 처리 (에러 상태에서 사용자가 무한 대기하는 것 방지)

### 결과
- 회의록이 완전히 생성된 후에만 "완료" 표시, 데이터 정합성 확보

## 6.6 이슈 6: 이어서 녹음 시 오디오 파일 충돌 (Race Condition)

### 증상
- 이어서 녹음 세션에서 기존 오디오 파일에 직접 append 시 파일 corruption 발생

### 원인 분석
- WebSocket `_write_audio_sync()`와 `finalize` 엔드포인트가 동시에 같은 파일에 접근
- 기존 파일 + 새 세션 파일을 단순 append하면 WebM 컨테이너 포맷 위반

### 해결 전략
1. **임시 파일 분리**: 이어서 녹음 세션은 `realtime_{id}_resume_{timestamp}.webm` 별도 저장
2. **FFmpeg concat**: 녹음 종료 시 `concat-resume` 엔드포인트에서 `filter_complex "[0:a][1:a]concat=n=2:v=0:a=1[a]"` 명령으로 오디오 스트림 합치기
3. **Duration 헤더 복구**: 합쳐진 파일에 대해 `_repair_audio_duration_sync()`로 FFmpeg remuxing 수행

### 결과
- 이전 세션과 새 세션의 오디오가 안전하게 병합, 타임라인 연속성 확보

---

# 7장. 성과 분석 및 평가

## 7.1 정량적 성과

| 항목 | 수치 |
|------|------|
| 구현된 API 엔드포인트 | 18개 |
| 프론트엔드 페이지 | 7개 |
| DB 테이블 | 6개 |
| Alembic 마이그레이션 | 11개 버전 |
| 백엔드 Python 코드 | ~3,500줄 |
| 프론트엔드 JS 코드 | ~3,500줄 |
| Docker 서비스 | 3개 (DB + Backend + LLM) |
| Python 패키지 의존성 | 25+ |
| 해결된 기술적 이슈 | 6건 |

## 7.2 정성적 성과

### 7.2.1 사용자 경험 (UX) 성과
- **원클릭 회의록**: "녹음 시작 → 녹음 종료" 두 번의 클릭만으로 구조화된 회의록 자동 생성
- **실시간 피드백**: 녹음 중 자막이 실시간으로 화면에 표시되어 녹음 상태 직관적 확인
- **비개발 직군 친화적**: 복잡한 설정 없이 브라우저에서 바로 사용 가능한 UI

### 7.2.2 기술적 성과
- **온프레미스 AI 파이프라인 완성**: 외부 API 의존 없이 STT + LLM을 로컬 GPU에서 독립 운영
- **비동기 아키텍처 고도화**: `asyncio.to_thread()`를 활용한 GPU 연산과 이벤트 루프의 완전한 분리
- **견고한 에러 처리**: 3단계 JSON 파싱 Fallback, 오디오 전처리 Fallback 체인, WebSocket 비정상 종료 시 데이터 보존

## 7.3 데이터 안정성

| 위험 시나리오 | 대응 전략 | 결과 |
|---------------|-----------|------|
| WebSocket 비정상 종료 | `WebSocketDisconnect` 핸들러에서 잔여 버퍼 처리 + 백그라운드 요약 실행 | ✅ 데이터 보존 |
| STT 처리 중 에러 | `_force_complete_meeting_sync()`으로 강제 완료 → 사용자 무한대기 방지 | ✅ 상태 일관성 |
| LLM 응답 파싱 실패 | 3단계 Fallback (json → ast → regex) + 원본 텍스트 포함 기본 구조 반환 | ✅ 빈 화면 방지 |
| 동일 파일 중복 업로드 | SHA-256 해시 비교 → 기존 파일 재사용 | ✅ 중복 방지 |
| 이어서 녹음 파일 충돌 | 임시 파일 분리 + FFmpeg concat | ✅ Race Condition 해결 |

## 7.4 통신 구조 고도화

### 7.4.1 WebSocket 안정화 전략

1. **이벤트 루프 보호**: 모든 Blocking 연산(STT 추론, DB 쓰기, 파일 I/O)을 `asyncio.to_thread()` 또는 `asyncio.create_task()`로 비동기화
2. **연결 상태 추적**: `ConnectionManager.disconnected_clients` 집합으로 끊긴 클라이언트 감지 → 전송 시도 전 확인
3. **전송 실패 내성**: `send_json()` 실패 시 `False` 반환만 하고 예외 전파 없음 → DB 저장은 이미 완료되었으므로 데이터 무손실
4. **Pre-warm**: WebSocket 연결 즉시 STT 모델을 백그라운드에서 GPU에 로드 → 첫 전사 지연 최소화

### 7.4.2 HTTP Range Request 지원

- `main.py`의 `/media/{file_path:path}` 엔드포인트에서 HTTP 206 Partial Content 지원
- 오디오 플레이어의 시크(Seek) 기능이 정상 동작하도록 `Content-Range`, `Accept-Ranges` 헤더 구현
- ETag 기반 캐시 전략으로 변경되지 않은 파일은 304 Not Modified 반환

---

# 8장. 결론 및 회고

## 8.1 최종 요약

LiveMeeting 프로젝트는 **AI 기반 실시간 회의록 자동 생성 시스템**으로서 다음과 같은 핵심 가치를 구현하였다:

1. **완전한 온프레미스 AI 파이프라인**: Faster-Whisper(STT) + EXAONE 3.5(LLM)을 Docker 컨테이너 내에서 GPU 가속으로 구동하여, 기업 데이터 주권을 100% 보장하는 독립 운영 환경을 구축함.

2. **실시간 + 배치 처리 이원화**: WebSocket 기반 실시간 녹음 시에는 속도 우선(Denoise 생략, 5초 버퍼)으로, 파일 업로드 시에는 정확도 우선(고품질 Denoise, 30초 청킹, LLM 교정)으로 처리하여 상황별 최적의 UX를 제공함.

3. **견고한 에러 처리 체계**: WebSocket 연결 끊김, STT 환각, LLM 파싱 실패 등 다양한 장애 시나리오에 대한 Fallback 전략을 구현하여, 어떤 상황에서도 사용자 데이터가 보존되고 시스템이 복구되는 안정성을 확보함.

4. **체계적 데이터 관리**: Alembic을 통한 11단계 스키마 진화 관리, SHA-256 파일 해시 중복 방지, Cascade 삭제를 통한 데이터 정합성 유지.

## 8.2 회고

### 잘된 점
- FastAPI의 비동기 기능과 `asyncio.to_thread()`를 적극 활용하여 GPU 연산과 이벤트 루프를 완전히 분리한 아키텍처 설계
- 온프레미스 STT 모델을 `medium` → `large-v3-turbo`로 업그레이드하면서 `.env` 설정으로 간편하게 전환 가능하도록 추상화
- 6건의 기술적 이슈를 체계적으로 분석하고 문서화한 트러블슈팅 이력 관리

### 한계점
- **부하 테스트 미수행**: 동시 사용자 수 증가 시의 성능 한계를 정량적으로 측정하지 못함
- **화자 분리(Diarization) 미구현**: 현재 모든 전사 세그먼트의 speaker가 "Unknown"으로 고정
- **CI/CD 파이프라인 부재**: Git Hook이나 GitHub Actions 등의 자동화된 빌드/테스트/배포 파이프라인이 구축되지 않음
- **프론트엔드 프레임워크 미사용**: Vanilla JS로 구현하여 컴포넌트 재사용성과 상태 관리에 한계

### 발전 방향
1. **화자 분리**: pyannote-audio 또는 NeMo 기반 Speaker Diarization 통합
2. **검색 기능**: pgvector를 활용한 임베딩 기반 유사 회의 검색
3. **다국어 지원**: Whisper의 multilingual 기능을 활용한 영어/일본어 회의록 지원
4. **회의록 템플릿**: 사용자 커스텀 템플릿 지원 (JSON 프롬프트 편집)
5. **모바일 대응**: PWA(Progressive Web App) 적용으로 모바일 환경에서의 접근성 향상
6. **리팩토링**: React/Vue 등 프레임워크 전환으로 프론트엔드 유지보수성 개선

---

# 부록. 소감문

## 개인 성장

LiveMeeting 프로젝트를 통해 가장 크게 성장한 부분은 **비동기 프로그래밍에 대한 깊은 이해**이다. Python의 `asyncio` 생태계, 특히 `asyncio.to_thread()`를 활용한 CPU-bound 작업의 이벤트 루프 분리를 실제 프로덕션 수준에서 적용하면서, 동시성(Concurrency)과 병렬성(Parallelism)의 차이를 체감할 수 있었다.

또한, AI 모델(Faster-Whisper, EXAONE)을 Docker 환경에서 GPU 가속과 함께 운영하면서, 모델 로딩 시간, VRAM 관리, 추론 지연 최적화 등 **AI 엔지니어링의 실무적 감각**을 쌓을 수 있었다. 특히, STT 환각(Hallucination) 문제를 `initial_prompt` 튜닝, RegEx 필터링, VAD 파라미터 조정 등 다층적 접근으로 해결한 경험은 AI 서비스의 품질 관리가 단순히 모델 성능 뿐만 아니라 전처리·후처리 파이프라인의 설계에 크게 의존한다는 교훈을 주었다.

## 아쉬운 점

프로젝트 일정상 **테스트 자동화**와 **CI/CD 파이프라인** 구축에 충분한 시간을 투자하지 못한 것이 가장 아쉽다. 특히 실시간 WebSocket 통신에 대한 자동화된 통합 테스트가 부재하여, 코드 변경 시 수동으로 전체 플로우를 검증해야 하는 비효율이 있었다.

프론트엔드를 Vanilla JavaScript로 구현한 것은 초기 개발 속도를 높였으나, 코드 규모가 커지면서(dashboard.js 44KB, recording.js 34KB) 상태 관리와 컴포넌트 재사용에 어려움을 겪었다. React나 Vue.js 같은 프레임워크를 도입했다면 유지보수성이 크게 개선되었을 것이다.

## 향후 목표

1. **백엔드 고도화**: 화자 분리(Speaker Diarization), 실시간 번역, 키워드 추출 등 AI 기능 확장
2. **프론트엔드 현대화**: React + TypeScript로 리팩토링하여 컴포넌트 기반 아키텍처 도입
3. **DevOps 체계화**: GitHub Actions CI/CD, Docker 이미지 자동 빌드, Kubernetes 배포 검토
4. **성능 최적화**: WebSocket 커넥션 풀링, STT 모델 배치 추론, Redis 기반 세션 캐싱
5. **사용자 피드백 반영**: 회의록 평가 시스템 도입으로 LLM 프롬프트 지속 개선

---

*본 보고서는 LiveMeeting 프로젝트(`c:\big20\git\live_meeting`)의 실제 소스 코드를 기반으로 작성되었습니다.*
